package randoop.test;

import java.util.ArrayList;
import java.util.List;
import java.util.StringJoiner;
import randoop.contract.ObjectContract;

/**
 * Manages the set of {@link ObjectContract} objects. Contracts are organized by arity.
 */
@org.checkerframework.framework.qual.AnnotatedFor("org.checkerframework.checker.resourceleak.ResourceLeakChecker")
public class ContractSet {

    /**
     * Each element is a list of all contracts of that arity. For example, the element at index 2 is a
     * list of contracts of arity 2. Used only for containment check.
     */
    private final @org.checkerframework.checker.calledmethods.qual.CalledMethods({}) List<List<ObjectContract>> contractMap = new ArrayList<>(1);

    /**
     * Returns the list of contracts with the given arity.
     *
     * @param arity the arity
     * @return the list of contracts with the given arity
     */
    @org.checkerframework.checker.calledmethods.qual.EnsuresCalledMethods(value = { "this.contractMap" }, methods = { "get" })
    @org.checkerframework.dataflow.qual.SideEffectFree
    public @org.checkerframework.checker.calledmethods.qual.CalledMethods({}) List<ObjectContract> getWithArity(@org.checkerframework.checker.calledmethods.qual.CalledMethods({}) ContractSet this,  @org.checkerframework.checker.calledmethods.qual.CalledMethods({}) int arity) {
        List<ObjectContract> contractList = contractMap.get(arity);
        if (contractList == null) {
            contractList = new ArrayList<>();
        }
        return contractList;
    }

    /**
     * Adds a contract to this set.
     *
     * @param contract the contract
     */
    @org.checkerframework.checker.calledmethods.qual.EnsuresCalledMethods(value = { "#1" }, methods = { "getArity" })
    @org.checkerframework.checker.calledmethods.qual.EnsuresCalledMethods(value = { "this.contractMap" }, methods = { "get", "size" })
    public void add(ObjectContract contract) {
        int arity = contract.getArity();
        while (arity >= contractMap.size()) {
            contractMap.add(new ArrayList<>(1));
        }
        List<ObjectContract> contractList = contractMap.get(arity);
        contractList.add(contract);
    }

    @org.checkerframework.checker.calledmethods.qual.EnsuresCalledMethods(value = { "this.contractMap" }, methods = { "isEmpty" })
    @org.checkerframework.dataflow.qual.Pure
    public  @org.checkerframework.checker.calledmethods.qual.CalledMethods({}) boolean isEmpty(@org.checkerframework.checker.calledmethods.qual.CalledMethods({}) ContractSet this) {
        return contractMap.isEmpty();
    }

    @org.checkerframework.checker.calledmethods.qual.EnsuresCalledMethods(value = { "this.contractMap" }, methods = { "size" })
    public @org.checkerframework.checker.calledmethods.qual.CalledMethods({}) String toString(@org.checkerframework.checker.calledmethods.qual.CalledMethods({}) ContractSet this) {
        int cardinality = 0;
        StringJoiner contractString = new StringJoiner(System.lineSeparator());
        int size = contractMap.size();
        for (int i = 0; i < size; i++) {
            List<ObjectContract> contractList = contractMap.get(i);
            if (!contractList.isEmpty()) {
                contractString.add(String.format("    arity %d: %s", i, contractList));
                cardinality += contractList.size();
            }
        }
        return String.format("ContractSet[size=%d]%n%s", cardinality, contractString.toString());
    }
}
